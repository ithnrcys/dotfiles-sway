// ~/.config/waybar/config.jsonc — two bars, greyscale, kaze port
[

// ────────────────────────── TOP BAR ──────────────────────────
{
  "name": "top",
  "layer": "top",
  "position": "top",
  "height": 28,

  "modules-left":   ["sway/window", "cpu", "memory", "temperature", "custom/updates"],
  "modules-center": ["custom/workspaces"],
  "modules-right":  ["clock", "custom/weather", "custom/wifi", "custom/bt", "custom/power"],

  // focused window — λ + app name, reverse-DNS ids shortened
  "sway/window": {
    "max-length": 40,
    "format": "<b>λ</b> {app_id}  <span size='13pt'>✧</span>  ",
  "rewrite": {
  "<b>λ</b> com\\.mitchellh\\.ghostty(.*)": "<b>λ</b> ghostty$1",
  "<b>λ</b> org\\.mozilla\\.firefox(.*)": "<b>λ</b> firefox$1",
  "<b>λ</b> (.*)": "<b>λ</b> $1"
  }
  },

  // workspaces — custom script: [n] brackets the focused one
  "custom/workspaces": {
    "exec": "~/.config/waybar/scripts/workspaces.sh",
    "format": "{}",
    "tooltip": false
  },

  "cpu":    { "interval": 1, "format": "<span size='9pt'></span> \u2009{usage}%  <span size='13pt'>✧</span>  " },
  "memory": { "interval": 2, "format": "<span size='11pt'>󰍛</span> \u2009{used:0.1f}G  <span size='13pt'>✧</span>  " },

  // CPU die temp — k10temp; hwmon number can shift after reboot,
  // re-check with: for i in /sys/class/hwmon/hwmon*; do echo "$i $(cat $i/name)"; done
  "temperature": {
    "hwmon-path": "/sys/class/hwmon/hwmon4/temp1_input",
    "interval": 2,
    "format": "<span size='11pt'>󱩱</span> \u2009{temperatureC}°C  <span size='13pt'>✧</span>  ",
    "critical-threshold": 85
  },

  // pending zypper updates (no-refresh count, every 30 min)
  "custom/updates": {
    "exec": "~/.config/waybar/scripts/updates.sh",
    "interval": 1800,
    "format": "<span size='11pt'>󰳢</span> \u2009{}"
  },

  "clock": {
    "format": "{:%a %d %b  %H:%M}  <span size='13pt'>✧</span>  ",
    "tooltip-format": "<tt>{calendar}</tt>"
  },

  // wttr.in — click for 3-day rofi forecast
  "custom/weather": {
    "exec": "~/.config/waybar/scripts/weather-module.sh --status",
    "interval": 900,
    "format": "{}  <span size='13pt'>✧</span>  ",
    "on-click": "~/.config/waybar/scripts/weather-module.sh --menu"
  },

  // wifi status icon — click for rofi network menu
  "custom/wifi": {
    "exec": "~/.config/waybar/scripts/wifi-module.sh --status",
    "interval": 5,
    "format": "{}  <span size='13pt'>✧</span>  ",
    "on-click": "~/.config/waybar/scripts/wifi-module.sh --menu"
  },

  // bluetooth — click for rofi device menu
  "custom/bt": {
    "exec": "~/.config/waybar/scripts/bt-module.sh --status",
    "interval": 5,
    "format": "{}  <span size='13pt'>✧</span>  ",
    "on-click": "~/.config/waybar/scripts/bt-module.sh --menu"
  },

  // power menu (rofi) — edge gap comes from CSS padding-right, not spaces
  "custom/power": {
    "format": "<span size='11pt'>󰤁</span>",
    "on-click": "~/.config/waybar/scripts/power-module.sh"
  }
},

// ──────────────────────── BOTTOM BAR ─────────────────────────
{
  "name": "bottom",
  "layer": "top",
  "position": "bottom",
  "height": 28,

  "modules-left":   ["custom/nowplaying"],
  "modules-center": ["custom/uptime", "network"],
  "modules-right":  ["tray", "custom/volume", "custom/brightness"],

  // playerctl → mpd fallback; empty when nothing plays
  "custom/nowplaying": {
    "exec": "~/.config/waybar/scripts/nowplaying.sh",
    "interval": 3,
    "max-length": 45
  },

  "custom/uptime": {
    "exec": "uptime -p | sed 's/up //'",
    "interval": 60,
    "format": "󰅐 \u2009{}  <span size='13pt'>✧</span>  "
  },

  "network": {
    "format-wifi": "󰖩 \u2009{essid}  <span size='13pt'>✧</span>  󰓅  {bandwidthDownBits}",
    "format-ethernet": "󰈀 \u2009{ipaddr}",
    "format-disconnected": "󰤭",
    "interval": 3
  },

  "tray": { "spacing": 8 },

  // thin-line lever slider ⎯⎯⎯◈ ; scroll/click wired to pamixer
  "custom/volume": {
    "exec": "~/.config/waybar/scripts/volume-bar.sh",
    "interval": 1,
    "format": "{}  <span size='13pt'>✧</span>  ",
    "on-scroll-up": "pamixer -i 2",
    "on-scroll-down": "pamixer -d 2",
    "on-click": "pamixer --toggle-mute"
  },

  // monitor brightness over DDC/CI (ddcutil, i2c)
  "custom/brightness": {
    "exec": "~/.config/waybar/scripts/brightness-module.sh",
    "interval": 5,
    "on-scroll-up": "ddcutil setvcp 10 + 10",
    "on-scroll-down": "ddcutil setvcp 10 - 10"
  }
}
]
